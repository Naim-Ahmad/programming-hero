const numbers = [1, 3, 4, 5, 65, 8, 10];
const doubled = numbers.map(num => num * 2);
const half = numbers.map(num => num / 2);
const friends = ['naim', 'sadekin', 'asadul'];
const length = friends.map(friend => friend.length)
console.log(length);


