// syntax
/* 1. splice(start) */
const arr = ['orange', 'banana', 'apple']
// arr.splice(1)
console.log(arr); // ['banana', 'apple']
/* 2. splice(start, deleteCount) */
// arr.splice(0, 2)
console.log(arr.splice())
/* 3. splice(start, deleteCount, item0) */